name: Call DevOps Pipeline

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  use-central:
    uses: redasaad123/CI-CD-GitHub-Action/.github/workflows/Build.yml@main

    with:
      app_name: Redan
